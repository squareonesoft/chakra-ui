declare type RequestData = {
    commit: string;
    repo: string;
};
export declare function getInfo(request: RequestData): Promise<{
    user: string | null;
    pull: number | null;
    links: {
        commit: string;
        pull: string | null;
        user: string | null;
    };
}>;
export {};
